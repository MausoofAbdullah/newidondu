<section>
    <div class="container"> 
        {{!-- <div class=" mb-4">
            <a href="/admin/add-products" class="btn btn-success mt-4 " >Add Product</a>
        </div> --}}
        <table class="table mt-1 mb-4" id="usersTable">
            <thead>
                <tr>
                    <th scope="col">Number</th>
                    <th scope="col">title</th>
                    <th scope="col">Category</th>
                    <th scope="col">Discription</th>
                    <th scope="col">date</th>
                    <th scope="col">Image</th>
                    <th scope="col">Options</th>
                </tr>
            </thead>
            <tbody>
                {{#each allnews}}
                <tr>
                    <th scope="row">{{inc @index}}</th>
                    <td>{{this.title}} </td>
                         <td>{{this.category}} </td>
                    <td>something </td>
                    <td>₹{{this.date}}</td>
                    <td><img src="https://res.cloudinary.com/dkeb469sv/image/upload/v1703658754/{{this.image1}}" style="width: 50px; height: 50px;"></td>
                
                    <td><a href="/editNews/{{this._id}}" class="btn btn-primary">Edit</a>
      <a href="/admin/delete-product/{{this._id}}" class="btn btn-danger" onclick="return confirm('Are you sure to dlete{{this.title}}?')">block</a></td>
    </tr>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

</section> 
